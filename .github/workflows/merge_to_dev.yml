---
name: Merge to Development

on:
  push:
    branches:
      - development    # Triggers when a PR is merged to development

jobs:
  deploy:
    uses: ./.github/workflows/deploy_dev.yml
    secrets: inherit
